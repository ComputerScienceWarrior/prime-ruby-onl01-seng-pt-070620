#1. Prime number is divisible ONLY by itself, and 1 (not divisible )
#primes always have a remainder

def prime?(number)
  
  while x < number
    if num % i == 0
      false
    x = 2
    i += 1
  end 
end